"""Self-improvement agent for PKM bridge.

Replaces the fixed retrospective pipeline with a multi-turn agent that can
read, reason about, and modify the system's own artifacts (skills, rules,
prompt, user model) using meta-tools.
"""
